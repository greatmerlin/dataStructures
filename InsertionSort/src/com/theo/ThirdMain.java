package com.theo;

import java.util.Arrays;

public class ThirdMain { // Insertion Sort, In place Algorithm, Stable Algorithm, Time Complexity: O(n^2)

/*    public static void main(String[] args) {

        int[] myArray = {20, 35, -15, 7, 55, 1, 22, 111, 2, 4, 34, 43, 32, -5};

        System.out.println(Arrays.toString(myArray));

        practiceInsertionSort(myArray);

        System.out.println(Arrays.toString(myArray));
    }

    public static void practiceInsertionSort(int[] array){
        for (int i = 1; i < array.length; i++){
            int temp = array[i];
            int j;
            for (j = i; j > 0 && array[j - 1] > temp; j--){
                array[j] = array[j - 1];
                array[j - 1] = temp;
            }
            array[j] = temp;
        }
    }*/
}
